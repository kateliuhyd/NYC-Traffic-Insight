# ── Core ML ───────────────────────────────────────────────
pandas>=2.0,<3
numpy>=1.24,<3
scikit-learn>=1.2,<2
joblib>=1.3,<2

# ── API ───────────────────────────────────────────────────
fastapi>=0.100,<1
uvicorn>=0.23,<1
pydantic>=2.0,<3

# ── Streamlit UI ──────────────────────────────────────────
streamlit>=1.32,<2

# ── Geo / Maps ────────────────────────────────────────────
folium>=0.15,<1
gdown>=5,<6

# ── Data pipelines (feature engineering) ──────────────────
holidays>=0.40,<1
requests>=2.28,<3

# ── GCS (optional, for cloud model storage) ───────────────
google-cloud-storage>=2.14,<3
